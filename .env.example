# Copy this file to .env and fill in your real values.
# .env should never be committed to version control.

SECRET_KEY=change-me-to-a-random-string

DB_USER=postgres
DB_PASSWORD=123456789
DB_HOST=localhost
DB_PORT=5432
DB_NAME=load_shedding

# Optional: overrides all of the DB_* values above if set directly
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/load_shedding
